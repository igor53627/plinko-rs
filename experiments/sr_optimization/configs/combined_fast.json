{
  "name": "combined_fast",
  "transforms": ["T1_PRECOMPUTE_KEYS", "T2_BATCH_AES", "T6_BATCH_ELEMENTS", "T7_RAYON_PARALLEL"],
  "params": {
    "security_bits": 128,
    "batch_size": 1024,
    "parallel": true
  },
  "description": "Full 128-bit security with batch AES + rayon parallelism (~20x speedup)"
}

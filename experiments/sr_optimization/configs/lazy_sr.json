{
  "name": "lazy_sr",
  "transforms": ["T1_PRECOMPUTE_KEYS", "T3_LAZY_SR"],
  "params": {
    "security_bits": 128,
    "batch_size": 1,
    "early_exit_threshold": 0.5
  },
  "description": "Lazy SR evaluation - skip deep levels when value exits early"
}
